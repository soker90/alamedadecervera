---
import {MENU} from "../config";
import Logo from "./Logo.astro";
---
<header class="text-white body-font bg-primary">
	<div class="container mx-auto flex flex-wrap p-5 flex-col md:flex-row items-center">
		<a class="flex title-font font-medium items-center mb-4 md:mb-0">
			<Logo />
			<span class="ml-3 text-xl">Alameda de Cervera</span>
		</a>
		<nav class="md:ml-auto flex flex-wrap items-center justify-center">
			{
				MENU.map(({title, href}) => <a class="mr-5 hover:text-gray-200" href={href}>{title}</a>)
			}
		</nav>
	</div>
</header>

